% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Transform_long.R
\name{Transform_long}
\alias{Transform_long}
\title{Transform Wide Format Results to Long Format}
\usage{
Transform_long(lWide, strDenominatorCol = "DenominatorType")
}
\arguments{
\item{lWide}{list. Named list of wide-format dataframes keyed by denominator
type (e.g., "Visits", "Days"). Each dataframe contains columns with
MetricID suffixes (e.g., \code{Median_kri0001}, \code{Lower_kri0001}).}

\item{strDenominatorCol}{character. Column name for denominator type in
output (default: "DenominatorType").}
}
\value{
A data.frame in long format with columns:
\itemize{
\item \code{MetricID}: Extracted from column suffixes (e.g., "kri0001")
\item \code{DenominatorType}: From list keys (e.g., "Visits", "Days")
\item Original id columns preserved
\item Pivoted value columns (e.g., \code{Median}, \code{Lower}, \code{Upper})
}
}
\description{
Converts wide-format dataframes from \verb{_Wide} workflow steps back into long
format with standard columns (\code{MetricID}, \code{Numerator}, \code{Metric}, etc.).
Auto-detects column patterns and extracts MetricID from suffixes.
}
\examples{
# Create example wide-format data
dfWide <- data.frame(
  StudyID = c("AA-1", "AA-2"),
  StudyMonth = c(1, 1),
  Median_kri0001 = c(0.5, 0.6),
  Lower_kri0001 = c(0.3, 0.4),
  Upper_kri0001 = c(0.7, 0.8),
  Median_kri0003 = c(0.2, 0.25),
  Lower_kri0003 = c(0.1, 0.15),
  Upper_kri0003 = c(0.3, 0.35),
  BootstrapCount = c(1000, 1000)
)

lWide <- list(Visits = dfWide)
dfLong <- Transform_long(lWide)
print(dfLong)

}
