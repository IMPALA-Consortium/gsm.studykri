% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AddScreeningFailures.R
\name{AddScreeningFailures}
\alias{AddScreeningFailures}
\title{Add Screening Failures to Enrolled Subjects}
\usage{
AddScreeningFailures(
  dfEnrolled,
  dfSource,
  strSiteCol = "siteid",
  strStudyCol = "studyid",
  seed = NULL
)
}
\arguments{
\item{dfEnrolled}{Dataframe containing only enrolled patients (enrollyn == "Y"),
modeled after \code{clindata::rawplus_enroll}. Must contain columns for study identifier,
site identifier, and \code{enrollyn}. Can contain data for multiple studies.}

\item{dfSource}{Dataframe containing both enrolled (Y) and screening failure (N) patients.
Serves as a patient pool and source of enrollment ratios. The study identifier in
\code{dfSource} is ignored - ratios are calculated per site only and used as examples
for sampling. Must contain the same columns as \code{dfEnrolled}.}

\item{strSiteCol}{Character string specifying the column name for site identifier.
Default: "siteid"}

\item{strStudyCol}{Character string specifying the column name for study identifier.
Default: "studyid"}

\item{seed}{Integer seed for reproducibility. Default: NULL (uses current random state)}
}
\value{
The \code{dfEnrolled} dataframe augmented with sampled screening failure records.
The returned dataframe contains both enrolled (Y) and screening failure (N) patients.
}
\description{
Augments a dataframe of enrolled subjects (enrollyn == "Y") with screening failures
(enrollyn == "N") sampled from a source dataframe. For each study-site combination in
the enrolled data, the function randomly selects an enrollment ratio from the source
data and samples the corresponding number of screening failures to achieve that ratio.
}
\details{
The function performs the following steps:
\enumerate{
\item \strong{Calculate enrollment ratios} per site in \code{dfSource}:
\itemize{
\item Groups by site and counts Y and N patients (study identifier is ignored)
\item \strong{Excludes sites with zero enrolled patients} to avoid division by zero
\item Computes ratio: n_enrolled / (n_enrolled + n_screening_failures)
\item Creates a pool of ratios from all sites with at least one enrolled patient
}
\item \strong{Process each study-site combination} in \code{dfEnrolled}:
\itemize{
\item Identifies unique study-site combinations
\item Counts enrolled patients at each specific study-site
\item Randomly selects one ratio from the pool (independent per study-site)
\item Calculates number of screening failures needed: n_enrolled * (1 - ratio) / ratio
}
\item \strong{Sample screening failures}:
\itemize{
\item Filters \code{dfSource} to only enrollyn == "N" patients
\item Samples calculated number \strong{with replacement} from entire pool (ignoring sites)
\item Updates both study and site identifiers to match the target study-site in \code{dfEnrolled}
}
\item \strong{Combine and return}:
\itemize{
\item Row-binds sampled screening failures to \code{dfEnrolled}
\item Returns combined dataframe
}
}

\strong{Example ratio calculation:}
If a study-site has 100 enrolled patients and the randomly selected ratio is 0.8
(80\% enrolled, 20\% screen failures), the function adds:
\code{100 * (1 - 0.8) / 0.8 = 25} screening failures

\strong{Multi-study handling:}
When \code{dfEnrolled} contains multiple studies, each study-site combination is processed
independently. The same site name can appear in multiple studies (e.g., "Site1" in
both "Study A" and "Study B"), and each will receive its own sampled screening failures.

\strong{Note:} Sampling is done with replacement, so the same screening failure record
from \code{dfSource} can appear multiple times in the output.
}
\examples{
# Single study example
dfEnrolled <- clindata::rawplus_enroll |>
  dplyr::filter(enrollyn == "Y")

# Source data with both Y and N
dfSource <- clindata::rawplus_enroll

# Add screening failures
dfAugmented <- AddScreeningFailures(
  dfEnrolled = dfEnrolled,
  dfSource = dfSource,
  strSiteCol = "siteid",
  strStudyCol = "studyid",
  seed = 123
)

# Check results
table(dfAugmented$enrollyn) # Should show both Y and N
nrow(dfAugmented) > nrow(dfEnrolled) # Should be TRUE

# Multi-study example
dfEnrolled_multi <- data.frame(
  studyid = c(rep("Study1", 50), rep("Study2", 30)),
  siteid = c(rep(c("Site1", "Site2"), each = 25), rep(c("Site1", "Site3"), each = 15)),
  enrollyn = "Y"
)

dfAugmented_multi <- AddScreeningFailures(
  dfEnrolled = dfEnrolled_multi,
  dfSource = dfSource,
  strSiteCol = "siteid",
  strStudyCol = "studyid",
  seed = 123
)

# Each study-site gets independent screening failures
table(dfAugmented_multi$studyid, dfAugmented_multi$siteid)

}
